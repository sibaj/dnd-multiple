import { MultipleDndDirective } from './multiple-dnd.directive';

describe('MultipleDndDirective', () => {
  it('should create an instance', () => {
    const directive = new MultipleDndDirective();
    expect(directive).toBeTruthy();
  });
});
